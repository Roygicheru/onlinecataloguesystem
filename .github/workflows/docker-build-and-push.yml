name: 🧩 Application Code Check

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: 🧪 Validate Frontend Code Directory
        run: |
          echo "Running simple CI check on frontend code..."
          # This command ensures the directory exists and lists its contents
          ls -la ./ocsreact 

      # You can add more detailed CI steps here later, like:
      # - name: Setup Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '20' 

      # - name: Install Dependencies and Run Tests
      #   working-directory: ./ocsreact
      #   run: |
      #     npm install
      #     npm test